#include "lib/stdlib.bc"

function main(var pr) {
  //print_number(num);
  var i = 0;
  while (i < 100) {
    pr(i);
    ++i;
  }
}

//var print_arg  = function(var arg) {
//  print_number(arg);
//  out(10);
//};

main(function(var arg) {
  print_number(arg);
  out(10);
});