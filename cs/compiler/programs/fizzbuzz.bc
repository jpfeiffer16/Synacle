#include "lib/stdlib.bc"

var i = 1;

while (i < 40) {
  var fizz = i % 3;
  var buzz = i % 5;

  if (fizz && buzz) {
    print_number(i);
  }

  if (!fizz) {
    print("fizz");
  }

  if (!buzz) {
    print("buzz");
  }

  i = i + 1;

  out(10);
}