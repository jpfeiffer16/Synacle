#include "stdlib.bc"

//print("Hello World!");

var i;
i = 1;

while (i < 40) {
  var fizz;
  var buzz;
  fizz = i % 3;
  buzz = i % 5;

  if (fizz && buzz) {
    print_number(i);
  }

  if (!fizz) {
    print("fizz");
  }

  if (!buzz) {
    print("buzz");
  }

  i = i + 1;

  out(10);
}