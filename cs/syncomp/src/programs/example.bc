#include "lib/stdlib.bc"
#include "lib/error.bc"
#include "lib/array_wrapper.bc"

function main()
{
  var ar = array_new(10);
  array_foreach(
    ar,
    function(elem_ptr, var index)
    {
      print_number(index);
      println(" ");
    }
  );
}

main();