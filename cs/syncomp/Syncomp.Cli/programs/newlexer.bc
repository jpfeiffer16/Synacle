#include "./lib/stdlib.bc"

type entity {
    int id,
    string data
};

type test {
    ptr<entity> ent
};

entity e;
ptr<entity> en = &e;

test tt;
ptr<test> tst = &tt;

tst->ent = en;

tst->ent->data;

// yay->test;
