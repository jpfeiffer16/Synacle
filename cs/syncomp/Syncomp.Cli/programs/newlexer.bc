#include "./lib/stdlib.bc"

type test
{
    int id
}

function main(ptr<test> yay)
{
    int size = sizeof(~yay);
    string name = nameof(yay);
    println(name);
    print_number(size);
}
test test_var;

main(&test_var);
