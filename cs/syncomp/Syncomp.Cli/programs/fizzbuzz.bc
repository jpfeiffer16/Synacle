#include "lib/stdlib.bc"

for (var i = 1; i < 40; ++i) {
  var fizz = i % 3;
  var buzz = i % 5;

  if (fizz && buzz) {
    print_number(i);
  }

  if (!fizz) {
    print("fizz");
  }

  if (!buzz) {
    print("buzz");
  }

  out(10);
}
