#include "lib/stdlib.bc";
#include "lib/libpig.bc"

function main() {
  print("Enter your name or 'exit' to stop: ");
  string name = input();
  println(name);
  if (str_equal(name, "exit")) {
    return;
  }
  var result = translate_word(name);
  println(result);
  main();
}

main();
