#include "./lib/stdlib.bc"

var test = () : void =>
{
    println("Hello World!");
};

print_number(test as int);
println(" ");

println("Enter address to call:");
var addr = int(input());
print_number(addr);

var funfunction = addr as func<void>;
funfunction();

var str_num = string(addr);
println(str_num);
