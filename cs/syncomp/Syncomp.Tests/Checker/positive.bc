type test
{
    int id,
    string name
}

// Here, we're trying to confuse the parser/typechecker on purpose

function main(func<ptr<test>, void> cb)
{
    function test(string str) {}
    ptr<test> &test_var;
    test_var->name = "Test";
}

main(function(ptr<test> person) {
});

//Lambdas
var lambda_test_one = () => {};
lambda_test_one = () => "Test";

var lambda_test_two = () : int => 123;
lambda_test_two = () : int => {
    return 123;
};
